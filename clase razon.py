class Razon:
    def Razones(transacciones):
        h = []
        for i in transacciones:
            
            if transacciones[i]['tipo'] == 'RETIRO_EFECTIVO_CAJERO_AUTOMATICO' 
                h.append = ("no hay saldo disponible para retirar ese monto")
